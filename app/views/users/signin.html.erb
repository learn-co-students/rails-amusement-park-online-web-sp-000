<h1>Log In</h1>

<%= form_with method: @user do |f| %>
	<%= f.collection_select :user_name, @users, :name, :name %><br>
	
	<%= f.label :password %>
	<%= f.text_field :password %><br>

	<%= f.submit "Sign In" %>
<% end %>
