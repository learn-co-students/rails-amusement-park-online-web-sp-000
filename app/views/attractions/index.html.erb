<h1>Listing Attractions</h1>
<tbody>
<tr>
<td>Name</td>
<td>Tickets</td>
<td>Details</td>
</tr>
<tr>
  <% @attractions.each do |a| %>
<td><%= a.name %></td>
<td><%= a.tickets %></td>
<td><%= link_to "Show #{a.name}", attraction_path(a) %></td>
<% end %>
</tr>
