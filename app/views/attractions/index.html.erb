<h1>Listing Attractions</h1>
<ul>
    <li>Name            Tickets         Description</li>
<%= @attractions.all.each do |attraction|%>
    <li><%=attraction.name%>            <%= attraction.tickets%> <%= link_to "Go on #{attraction.name}", attraction_path(attraction)%></li>
<%end%>
</ul>
