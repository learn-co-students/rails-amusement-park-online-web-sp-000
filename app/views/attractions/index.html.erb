<h1>Attractions</h1><br>
<ul>
<% @attractions.each do |att| %>
  <% if @user.admin %>
    <li><%= link_to "Show #{att.name}", attraction_path(att) %>
  <% else %>  
    <li><%= link_to "Go on #{att.name}", attraction_path(att) %>
  <% end %>
<% end %><br><br>

<% if @user.admin %>
 <%= link_to "New Attraction", new_attraction_path %>
<% end %>
</ul>