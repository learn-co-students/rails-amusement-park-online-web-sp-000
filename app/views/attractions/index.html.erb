<h1>Attractions</h1>

<%= Attraction.all.each do |attraction| %>
    <p><%= link_to "#{@verb} #{attraction.name}", attraction_path(attraction) %></p>
<% end%>

<% if current_user.admin? %>
    <%= link_to "New Attraction", new_attraction_path %>
<% end %>