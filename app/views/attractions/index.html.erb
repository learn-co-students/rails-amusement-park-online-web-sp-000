<h1>Here are all the attractions!</h1>
<ul>
  <% @attractions.each do |attraction| %>
    <li>
      <%= link_to "Show #{attraction.name}", attraction_path(attraction) %> - <%= link_to "Go on #{attraction.name}", attraction_path(attraction) %>
    </li>
  <% end %>
</ul>