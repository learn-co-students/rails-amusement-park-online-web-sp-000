<h1> All the attractions! </h1>
  <% @attractions.each do |attraction| %>
    <%= link_to "Go on #{attraction.name}", attraction_path(attraction) if !@user.admin %><br>
    <%= link_to "Show #{attraction.name}", attraction_path(attraction) if @user.admin %><br>
  <% end %>

  <%= link_to "New Attraction", new_attraction_path %>
