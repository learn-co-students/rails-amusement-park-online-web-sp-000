<%= @user.admin ? (link_to "New Attraction", new_attraction_path) : nil %>

<% @attractions.each do |attraction| %>
  <%= @user.admin ? (link_to "Show #{attraction.name}", attraction_path(attraction)) : (link_to "Go on #{attraction.name}", attraction_path(attraction)) %><br />
<% end %>
