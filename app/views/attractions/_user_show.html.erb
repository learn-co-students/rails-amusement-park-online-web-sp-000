
Tickets Required: <%= @attraction.tickets%> 

<%= form_tag ride_path do %>
    <%= hidden_field_tag(:attraction_id, @attraction.id) %>
    <%= hidden_field_tag(:user_id, session[:user_id]) %>
    <%= submit_tag "Go on this ride" %>
  <% end %>