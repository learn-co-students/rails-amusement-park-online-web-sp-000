<p>Ride: <%= @attraction.name %></p>
<p>Minimum Height: <%= @attraction.min_height %></p>
<p>Nausea Rating (1-5): <%= @attraction.nausea_rating %></p>
<p>Happines Rating (1-5): <%= @attraction.happiness_rating %></p>
<p>Tickets Required: <%= @attraction.tickets %></p> 

<%= form_for Ride.new do |f| %> 
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.hidden_field :attraction_id, :value => @attraction.id %>
    <br>
    <%= f.submit "Go on this ride" unless !!current_user.admin?%>
<% end %>

<%= link_to "Edit Attraction", edit_attraction_path if !!current_user.admin? %>

