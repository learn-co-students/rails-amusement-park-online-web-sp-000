<h1><%= @attraction.name %></h1>

<% if @user.admin == true %>
  <%= link_to 'Edit Attraction', edit_attraction_path(@attraction), class: "btn btn-primary" %>
<% else %>
<%= button_to 'Go on this ride', ride_path(@attraction), method: :post, class: "btn btn-primary" %>
<% end %>