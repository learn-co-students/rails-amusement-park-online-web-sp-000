You're riding the <%= @attraction.name %>
<% if @admin %>
    <%= link_to "Edit Attraction", edit_attraction_path(@attraction) %>
<% else %>
    <%= form_for @attraction, :url => {:action=> 'takeride'} do |f| %>
        <%= f.submit "Go on this ride." %>
    <% end %>
<% end %>

