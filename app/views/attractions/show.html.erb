<% if is_admin? %>
    <%= link_to 'Edit Attraction', edit_attraction_path(@attraction) %>
<% end %>

<%= @attraction.name %>
<%= button_to "Go on this ride", rides_path(@ride), params: ['ride[user_id]' => @user.id, 'ride[attraction_id]' => @attraction.id] %>