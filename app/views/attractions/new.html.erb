

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@attraction) do |f| %>
      <%= render 'attractions/attraction', :f => f  %>
      <%= f.submit "Create Attraction", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>