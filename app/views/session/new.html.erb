<h1> Log-in Prompt </h1>
<%= form_for @user do |f| %>
    <% binding.pry %>
    <p>User: <%= collection_select(:user, :name, User.all, :id, :name) %></p>
    <p>Password: <%= f.password_field :password %></p>
    <%= f.submit 'Sign In' %>
<% end %>