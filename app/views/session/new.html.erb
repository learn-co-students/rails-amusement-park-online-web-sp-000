<%= form_for @user, url: signin_path(@user) do |f| %>
    <%= f.collection_select :name, User.all, :name, :name %><br><br>
    <%= f.label :password %><br>
    <%= f.password_field :password %><br>
    <%= f.submit 'Sign In' %>
<% end %>