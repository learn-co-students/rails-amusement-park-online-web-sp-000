<%= form_for(@user) do |f| %>
    <%= f.label :name %>
    <%= f.select :name, options_from_collection_for_select(@users, :name, :name) %><br>
    <%= f.label :password %>
    <%= f.password_field :password, id: 'password' %><br>
    <%= f.submit 'Sign In' %>
<% end %>