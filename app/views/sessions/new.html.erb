<%= form_tag(sessions_path) do %>
    <%= collection_select(:user, :name, User.all, :name, :name) %>
    <%= text_field_tag :user_password, "user_password", placeholder: "password" %>
    <%= submit_tag 'Sign In' %>
<% end %>