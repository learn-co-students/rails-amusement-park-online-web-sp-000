You made it to the sign-in page. It is a work in progress.

<%= form_tag('/sessions') %>
<%= select_tag "user_name", options_from_collection_for_select(@users, "id", "name") %><br>
Password: <%= password_field_tag %><br>
<%= submit_tag "Sign In" %>

