<h1> Log-in Prompt </h1>
<%= form_for @user, url: sessions_path(@user) do |f| %>
    <p>User: <%= f.collection_select(:name, User.all, :name, :name) %></p>
    <p>Password: <%= f.password_field :password %></p>
    <%= f.submit 'Sign In' %>
<% end %>