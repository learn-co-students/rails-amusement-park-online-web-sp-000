<h1>Sign In</h1>

<%= form_for :user do |f| %>
  <p><%= f.label :user_name %>
  <%= f.collection_select :name, User.all, :id, :name %></p>
  
  <p><%= f.label :password %>
  <%= f.password_field :password, id: 'password' %></p>
  <p><%= f.submit 'Sign In' %></p>
<% end %>
