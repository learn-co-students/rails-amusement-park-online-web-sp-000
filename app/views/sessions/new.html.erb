<%= form_tag(sessions_path) do %>

  <%= label_tag(:name, "Select User")  %>
  <%= collection_select(:user, :name, User.all, :name, :name) %><br>
  <%= label_tag(:password, "password") %>
  <%= password_field_tag(:password) %><br>
  <%= submit_tag("Sign In") %>
<% end %>
