<%= form_with model: @sessions, url: '/signin', method: 'post' do |f| %>
    <%= f.label :name %>
    <%= f.collection_select :user_name, User.all, :id, :name %>
   
    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit "Sign In"%>
<% end %>