<%= form_tag do %>
  Please select your name from the dropdown:
  <%= select_tag :user_name, options_from_collection_for_select(User.all, :id, :name) %><br>
  Password:
  <%= password_field_tag :password %>
  <%= submit_tag "Sign In" %>
<% end %>
