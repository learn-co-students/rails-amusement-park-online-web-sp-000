<h1>Log In</h1>

<%= form_tag sessions_path do %>

		<%= select_tag 'user_name', options_for_select(@users.collect{ |u| [u.name, u.name] }) %>

    <%= label_tag :password %><br />
    <%= password_field_tag :password %>

		<%= submit_tag "Sign In" %>
<% end %>